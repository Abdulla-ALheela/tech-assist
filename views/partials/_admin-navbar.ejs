<nav>
    <% if(user) { %>
    <a href="/users/<%= user._id %>/admins">Your Assigned Requests</a>
    <a href="/users/<%= user._id %>/admins/master">Make an admin</a>
    <a href="/auth/sign-out">Sign Out</a>
    <% } else { %>
    <% } %>
  </nav>